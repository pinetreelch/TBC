#include <stdio.h>


int main()
{
    //** tasks.json 생성
    // cmd + shift + B -> C/C++: gcc 활성 파일 필드의 톱니바퀴 클릭 => .vscode 폴더 하위에 tasks.json 파일 생성

    //** launch.json 파일 생성
    // 소스파일에서 F5 클릭 후 , LLDB 선택 
    
    //F5->LLDB를 룰러서 디버깅모드에 진입
    
    printf("Hello World \n");
    return 0;
}